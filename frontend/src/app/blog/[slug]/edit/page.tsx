'use client';

// Reuses the create page component - the slug param triggers edit mode
import BlogEditorPage from '@/app/blog/create/page';

export default function BlogEditPage() {
  return <BlogEditorPage />;
}
